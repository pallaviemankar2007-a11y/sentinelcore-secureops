spring.application.name=infrastructure-monitoring
server.port=8080

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/sentinelcore_db
spring.datasource.username=postgres
spring.datasource.password=Shalini7!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
jwt.secret=MySecureOpsJwtSecretKeyForAuthentication2026VerySecure
spring.sql.init.mode=always

# JPA/Hibernate ko pehle tables create karne do
spring.jpa.defer-datasource-initialization=true

# SQL script failure ignoring (optional for dev)
spring.sql.init.continue-on-error=true